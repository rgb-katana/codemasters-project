import { RemoveHyphenPipe } from './remove-hyphen.pipe';

describe('RemoveHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
