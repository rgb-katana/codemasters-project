import { SplitByColonPipe } from './split-by-colon.pipe';

describe('SplitByColonPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitByColonPipe();
    expect(pipe).toBeTruthy();
  });
});
